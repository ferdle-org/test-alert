[project]
name = "<name of my project>"
version = "0.1.0"
description = "<description of my project>"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pygithub>=2.9.1",
    "python-dotenv>=1.2.2",
    "requests>=2.34.2",
]

[dependency-groups]
dev = [
    "mypy>=2.3.0",
    "pytest>=9.1.1",
    "pytest-bandit>=0.6.1",
    "pytest-cov>=7.1.0",
]
