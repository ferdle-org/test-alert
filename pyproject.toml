[project]
name = "test-alert"
version = "0.1.0"
description = "Alerting utility for monitoring and notifications."
readme = "README.md"
requires-python = ">=3.11"
authors = [
  { name = "Your Name", email = "you@example.com" }
]
license = { text = "MIT" }
dependencies = [
  "PyGithub>=2.9.1",
  "python-dotenv>=1.2.2",
  "requests>=2.34.2",
]

[dependency-groups]
dev = [
  "mypy>=1.11.0",
  "pytest>=8.3.0",
  "pytest-bandit>=0.6.1",
  "pytest-cov>=5.0.0",
]
